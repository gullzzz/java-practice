# 阶段 4：数组

配套代码：`src/com/practice/arrays/ArraysDemo.java`

---

## 4.1 数组的基本概念

数组是**同一类型**元素的**定长**有序集合。

```java
// 方式1：声明 + 分配
int[] arr = new int[5];   // 分配5个int空间，默认值0
arr[0] = 10;

// 方式2：声明 + 初始化
int[] arr = {1, 2, 3, 4, 5};
String[] names = {"张三", "李四", "王五"};
```

核心特性：
- **长度固定**：创建后不可改变
- **索引从 0 开始**：`arr[0]` 是第一个元素
- 访问越界会抛 `ArrayIndexOutOfBoundsException`
- `arr.length` 获取长度（属性，不是方法）
- 数组是**引用类型**，存储在堆上

---

## 4.2 数组遍历

```java
int[] scores = {85, 92, 78};

// 普通 for（有索引，可修改元素）
for (int i = 0; i < scores.length; i++) {
    scores[i] += 5;  // 可以修改
    System.out.println(scores[i]);
}

// for-each（无索引，无法修改元素）
for (int s : scores) {
    // s 是副本，修改 s 不影响原数组
    System.out.println(s);
}
```

---

## 4.3 二维数组

```java
// 声明
int[][] matrix = {
    {1, 2, 3},
    {4, 5, 6},
    {7, 8, 9}
};

// 遍历
for (int i = 0; i < matrix.length; i++) {       // 行
    for (int j = 0; j < matrix[i].length; j++) { // 列
        System.out.print(matrix[i][j] + " ");
    }
}
```

Java 的二维数组本质是**数组的数组**：
- `matrix` 是 `int[][]`
- `matrix[0]` 是 `int[]`
- `matrix[0][0]` 是 `int`

因此支持**不规则数组**（每行列数可以不同）：
```java
int[][] jagged = {
    {1},
    {2, 3},
    {4, 5, 6}
};
```

---

## 4.4 Arrays 工具类

`java.util.Arrays` 提供常用数组操作：

| 方法 | 作用 | 示例 |
|------|------|------|
| `toString(arr)` | 转为字符串 | `[1, 2, 3]` |
| `sort(arr)` | 原地排序（升序） | `{3,1,2} → {1,2,3}` |
| `binarySearch(arr, key)` | 二分查找 | 返回索引，未找到返回负数 |
| `fill(arr, val)` | 填充值 | 所有元素变为指定值 |
| `copyOf(arr, len)` | 复制数组 | 返回新数组 |
| `copyOfRange(arr, from, to)` | 部分复制 | `[from, to)` |
| `equals(arr1, arr2)` | 比较内容 | true/false |

**重要**：`binarySearch` 要求数组**已排序**。

---

## 4.5 常见算法

### 找最大/最小值
```java
int max = arr[0];
for (int v : arr) {
    if (v > max) max = v;
}
```

### 反转数组
```java
for (int i = 0, j = arr.length - 1; i < j; i++, j--) {
    int temp = arr[i];
    arr[i] = arr[j];
    arr[j] = temp;
}
```

### 冒泡排序
时间复杂度 O(n²)，适合理解排序原理：
```java
for (int i = 0; i < arr.length - 1; i++) {
    for (int j = 0; j < arr.length - 1 - i; j++) {
        if (arr[j] > arr[j + 1]) {
            // 交换 arr[j] 和 arr[j+1]
        }
    }
}
```

---

## 4.6 数组 vs 集合

| | 数组 | 集合(ArrayList等) |
|------|------|------|
| 长度 | 固定 | 可变 |
| 元素类型 | 基本类型或引用类型 | 只能引用类型 |
| 泛型 | 不支持 | 支持 |
| 性能 | 更快 | 稍慢（有额外开销） |
| 工具方法 | 少（Arrays类辅助） | 丰富 |

Java 中更推荐使用集合，将在阶段 8 详细讲解。

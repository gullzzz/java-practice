# 学习进度

> 最后更新时间：2026-09-07

---

## 🚀 一个月冲刺计划（2026-09-07 ~ 2026-10-06）

> 目标：学完 JavaSE 剩余 + Web 全链路，产出可展示项目 + 简历 + 面试准备。强度拉满。

| 阶段 | 日期 | 主题 | 核心任务 |
|------|------|------|---------|
| SE | 9/07 | 复习唤醒 + 同步锁 ✅ | 六大块复习唤醒 + synchronized/Lock 已通关 |
| SE | 9/08 | 并发集合 ✅ | ConcurrentHashMap / CopyOnWriteArrayList（9/07 提前完成） |
| SE | 9/09 | 线程通信+JUC | wait/notify + CountDownLatch/CyclicBarrier/Semaphore |
| SE | 9/10 | 内存模型+异步 | CAS/volatile/ThreadLocal + CompletableFuture |
| SE | 9/11 | 反射机制 | Class/Method/Field，Spring IoC 基石 |
| SE | 9/12 | 网络编程+序列化 | TCP/UDP/Socket + 数据流/对象流 |
| SE | 9/13 | OOP补漏 | static/this/super/final/instanceof + System + StringBuffer |
| Web | 9/14 | HTTP+RESTful+JSON | GET/POST/状态码/接口规范 |
| Web | 9/15 | Maven | pom.xml/依赖管理/打包 |
| Web | 9/16-17 | Spring IoC/DI + MVC | Bean容器/自动装配/@Controller/路由 |
| Web | 9/18-19 | RESTful接口实战 | @RestController/JSON/全局异常/拦截器 |
| Web | 9/20 | Spring Boot 小项目 | 串练巩固 |
| Web | 9/21-22 | MySQL | 建表/索引/查询优化/Explain |
| Web | 9/23-24 | MyBatis | SQL映射/动态SQL/整合 |
| Web | 9/25-26 | 三件套整合 | SpringBoot+MySQL+MyBatis CRUD |
| 项目 | 9/27-10/02 | 综合项目 🚀 | 可展示的全栈后端项目（6天） |
| 求职 | 10/03-10/06 | 简历+面试冲刺 | 简历打磨 + 八股文 + 项目亮点包装（4天） |

> 注：① 虚拟线程（JDK21+）暂缓，面试冲刺再补 ② 综合项目与简历/面试必须留足 10 天——这是最终产出物，权重最高。

---

## 整体进度
- 已学章节：27
- 进行中：多线程系列 —— ①线程创建✅ ②线程池✅ ③同步锁✅ ④并发集合✅ ⑤线程通信✅ → 当前：JUC工具类(CountDownLatch/CyclicBarrier/Semaphore)
- 超级整合项目：魔法交易所 3.0 ✅ 全部完成
- 当前阶段：一个月冲刺 —— JavaSE 收官 + Web 全链路 → 综合项目
- 📋 参照标准：黑马程序员《Java基础案例教程（第3版）》2025版大纲

---

## 基础补漏清单 🆕

> 对照黑马大纲发现的遗漏知识点，按优先级排列

### 🔴 P0 — 必须先补（影响后续学习）

| 遗漏 | 所属章节 | 一句话说明 |
|------|---------|-----------|
| **static** | OOP | 静态变量/方法/代码块，类级别 vs 实例级别 |
| **this / super** | OOP | 当前对象引用 / 父类引用，构造器链 |
| **final** | OOP | 不可变变量、禁止重写的方法、不可继承的类 |
| **instanceof** | 多态 | 运行时类型判断，向下转型前的安全检查 |
| **System 类** | Java API | arraycopy、currentTimeMillis、exit、gc |
| **StringBuffer** | 字符串 | 线程安全的 StringBuilder，append 性能对比 |

### 🟡 P1 — 应该补（面试高频/框架前置）

| 遗漏 | 所属章节 | 一句话说明 |
|------|---------|-----------|
| **数据流 / 对象流** | IO | DataStream 读写基本类型、ObjectStream 序列化 |
| **反射机制** | 新章 | Class、Method、Field，Spring IoC 的基石 |
| **yield / 线程优先级** | 多线程 | 线程让步、优先级调度 |

### 🟢 P2 — 可以补（加分项）

| 遗漏 | 所属章节 | 一句话说明 |
|------|---------|-----------|
| **网络编程** | 新章 | TCP/UDP、Socket，后端必备常识 |
| **JDBC 基础** | 数据库前置 | Connection、Statement、ResultSet，MyBatis 底层 |

---

## 实习备战路线图

> 更新：2026-06-18 | 基于四份 JD 交叉分析制定

### 技能差距一览

| ✅ 已掌握（4/4 JD覆盖） | 🔲 待学（4/4 JD覆盖） |
|------------------------|---------------------|
| OOP / 接口 / 抽象类 / 多态 | **多线程** — 优先级最高 |
| 集合 / 泛型 | **Spring Boot + SpringMVC** |
| 异常处理 / IO流 | **MySQL + SQL优化** |
| Lambda / Stream / Optional | **MyBatis** |
| 枚举 / 日期时间 / 正则 | **Maven 构建** |
| 包装类 / Object / Math | **RESTful + JSON** |
| Git 基础 | **Redis 缓存**（进阶） |

### 六阶段学习计划

```
第一阶段 · 多线程 ⭐ 当前
├── ① 线程创建（Thread / Runnable） ✅ 已完成
├── ② yield / 线程优先级 ✅ 已完成
├── ③ 线程池（Executors / ThreadPoolExecutor） ✅ 已完成
├── ④ 同步锁（synchronized / ReentrantLock） ✅ 已完成
├── ⑤ 并发集合（ConcurrentHashMap / CopyOnWriteArrayList） ✅ 已完成
├── ⑥ 线程通信（wait/notify / Condition） ✅ 已完成
├── ⑦ JUC 工具类 ⭐ 当前
│   ├── CountDownLatch（倒计时门闩）
│   ├── CyclicBarrier（循环栅栏）
│   └── Semaphore（信号量）
├── ⑧ CAS / volatile / ThreadLocal 🆕
├── ⑨ CompletableFuture（异步编排）
└── ⑩ 虚拟线程 Virtual Threads（JDK 21+）🆕

第二阶段 · 基础补漏 + 工程化
├── 🔴 P0 速补
│   ├── static / this / super / final / instanceof（补到 OOP 章 README）
│   ├── System 类（补到 Java API）
│   └── StringBuffer（补到字符串）
├── 🟡 反射机制 🆕（Spring IoC 前置，面试必问）
├── HTTP 基础（GET/POST、Restful 规范、JSON）
├── Maven（pom.xml、依赖管理、打包）
└── Spring Boot
    ├── IoC/DI（Bean容器、自动装配）
    ├── SpringMVC（@Controller、路由绑定、参数校验）
    ├── RESTful 接口（@RestController、JSON 序列化）
    └── 全局异常处理 + 拦截器

第三阶段 · 数据库 + ORM
├── 🟢 JDBC 基础 🆕（Connection/Statement/ResultSet）
├── MySQL（建表、索引、查询优化、Explain）
└── MyBatis（SQL映射、动态SQL、SpringBoot整合）

第四阶段 · 综合项目
└── SpringBoot + MySQL + MyBatis 全栈后端
    覆盖：RESTful API / CRUD / 分页 / SQL优化
    产出：「可展示可运行代码」

第五阶段 · 进阶（面向岗位2/4）
├── Redis 缓存
├── 🟢 网络编程 🆕（TCP/UDP、Socket）
├── Linux 常用命令
├── Docker 基础
└── 设计模式深入 + UML

第六阶段 · 面试冲刺
├── 简历打磨（魔法交易所 + 综合项目）
├── 八股文专题（Java基础 / 集合 / 多线程 / MySQL）
└── 项目亮点包装
```

---

## 已完成章节清单

### 第一阶段：基础入门
| 知识点 | 学习日期 | 状态 | Demo路径 | Test路径 |
|--------|----------|------|----------|----------|
| HelloWorld & 程序结构 | 2026-05-11 | ✅ | src/com/practice/HelloWorld.java | — |
| 变量与数据类型 | 2026-05-11 | ✅ | src/com/practice/basics/BasicsDemo.java | BasicsTest.java |
| 运算符 | 2026-05-11 | ✅ | — | — |
| 控制流程 | 2026-05-11 | ✅ | src/com/practice/controlflow/ControlFlowDemo.java | ControlFlowTest.java |
| 方法定义与调用 | 2026-05-11 | ✅ | src/com/practice/methods/MethodsDemo.java | MethodsTest.java |
| 数组 | 2026-05-11 | ✅ | src/com/practice/arrays/ArraysDemo.java | ArraysTest.java |
| 字符串 | 2026-05-14 | ✅ | —（视频学习） | — |

### 第二阶段：面向对象核心
| 知识点 | 学习日期 | 状态 | Demo路径 | Test路径 |
|--------|----------|------|----------|----------|
| 类与对象、构造方法、封装 | 2026-05-11 | ✅ | src/com/practice/oop/ | OopTest.java |
| 继承 | 2026-05-11 | ✅ | src/com/practice/inheritance/ | InheritanceTest.java |
| 接口 | 2026-05-11 | ✅ | src/com/practice/interfaces/ | InterfaceTest.java |
| 多态 | 2026-05-27 | ✅ | src/com/practice/polymorphism/ | — |
| 抽象类 | 2026-05-27 | ✅ | src/com/practice/abstractclass/ | — |
| 内部类 | 2026-05-27 | ✅ | src/com/practice/innerclass/ | — |
| 枚举 | 2026-05-17 | ✅ | src/com/practice/enums/ | — |

### 第三阶段：Java核心API
| 知识点 | 学习日期 | 状态 | Demo路径 | Test路径 |
|--------|----------|------|----------|----------|
| Object类 | 2026-05-18 | ✅ | src/com/practice/object/ | — |
| 包装类 | 2026-05-19 | ✅ | src/com/practice/wrappers/ | GoldCounter |
| 异常处理 | 2026-05-19 | ✅ | src/com/practice/exceptions/ | BankTransfer |
| 日期时间 (java.time) | 2026-05-20 | ✅ | src/com/practice/datetime/ | WorkshopScheduler |
| Math & Random | 2026-05-21 | ✅ | src/com/practice/mathrandom/ | AlchemistApp / SeedDebugger |
| 正则表达式 | 2026-05-26 | ✅ | src/com/practice/regex/ | PotionRecipeCleaner |
| 泛型 | 2026-05-27 | ✅ | src/com/practice/generics/ | GenericsTest |
| 集合框架 | 2026-05-14 | ✅ | src/com/practice/collections/ | ListTest / DictDemo / TaskCenter / CollectionMasterTest |

### 第四阶段：Java高级特性
| 知识点 | 学习日期 | 状态 | Demo路径 | Test路径 |
|--------|----------|------|----------|----------|
| Java 17 新特性 | 2026-05-11 | ✅ | src/com/practice/java17/ | Java17Test.java |
| Lambda 表达式 | 2026-05-31 | ✅ | src/com/practice/lambda/ | TradeAnalyzer |
| Stream API | 2026-06-01 | ✅ | src/com/practice/stream/ | StreamTradeAnalyzer |
| Optional | 2026-06-02 | ✅ | src/com/practice/optional/ | TradeQueryService |

---

## 已完成章节清单（续）

**4.1 Lambda 表达式** — 2026-05-31 完成

| 子章节 | 状态 | 说明 |
|--------|------|------|
| Lambda 语法（3种形态） | ✅ 已掌握 | 无参/单参/多参，表达式体 vs 语句块体 |
| 函数式接口 | ✅ 已理解 | 目标类型推断，Lambda = 函数式接口的匿名实现 |
| Predicate\<T\> | ✅ 已掌握 | test(T) 返回 boolean，过滤/条件判断 |
| Function\<T,R\> | ✅ 已掌握 | apply(T) 返回 R，提取/转换/映射 |
| Consumer\<T\> | ✅ 已掌握 | accept(T) 无返回值，消费/打印/存储 |
| Supplier\<T\> | ✅ 已掌握 | get() 无参有返回值，工厂/懒加载/生成 |
| 变量捕获 | ✅ 已理解 | 局部变量必须 effectively final |
| 防御性编程 | ✅ 已实践 | null 检查 + ArrayList 预扩容 + count 校验 |

---

**4.2 Stream API** — 2026-06-01 完成

| 子章节 | 状态 | 说明 |
|--------|------|------|
| Stream 创建（stream/Arrays.stream/Stream.of） | ✅ 已掌握 | 从集合/数组/可变参数打开流水线 |
| 中间操作（filter/map/sorted/distinct/limit） | ✅ 已掌握 | 惰性搭管道，返回 Stream 可链式调用 |
| 终端操作（collect/forEach/sum/anyMatch） | ✅ 已掌握 | 触发执行，返回非 Stream 类型，只能消费一次 |
| IntStream（mapToInt + sum） | ✅ 已掌握 | 避开了 Stream<Integer> 的装箱开销 |
| sorted 两种形态（自然序 vs Comparator） | ✅ 已掌握 | 自然序靠 Comparable，自定义靠 Comparator |
| 短路操作（anyMatch/limit） | ✅ 已理解 | anyMatch 找到即停，limit 截断上游 |
| 类型推断边界（comparingInt + reversed） | ✅ 已踩坑 | Lambda 穿透失败 → 方法引用破解 |
| 方法引用 :: | ✅ 已掌握 | 静态方法/实例方法/构造器引用，Lambda 的语法糖 |

**3.6 集合框架** — 2026-05-14 完成

| 子章节 | 状态 | 说明 |
|--------|------|------|
| List（ArrayList/LinkedList） | ✅ 已完成 | ListTest 5题，排序/反转/增删/去重/遍历 |
| Set（HashSet/TreeSet） | ✅ 已完成 | WordLibrary 挑战，大小写不敏感去重词库 |
| Map（HashMap/TreeMap） | ✅ 已完成 | DictDemo，put/get/containsKey/entrySet |
| Queue / Deque | ✅ 已完成 | TaskCenter，offerFirst/offerLast/pollFirst/peekFirst |
| 遍历方式 | ✅ 已完成 | CollectionMasterTest，for-each/Iterator/forEach+Lambda |
| Collections 工具类 | ✅ 已完成 | max/min/frequency/sort 等工具方法 |

---


## 章节完成规则
- ✅ = 理论已学 + Demo已跑通 + **练习题已全部完成并通过review**

---

## 待学章节

| 阶段 | 知识点 | 优先级 | 状态 |
|------|--------|:--:|:--:|
| 1 | **多线程** — Thread/Runnable、线程池、synchronized/Lock、并发集合、CompletableFuture | 🔴 P0 | 🔲 |
| 2 | **Maven** — pom.xml、依赖管理、打包 | 🔴 P0 | 🔲 |
| 2 | **HTTP + RESTful** — GET/POST、状态码、JSON 交互 | 🔴 P0 | 🔲 |
| 2 | **Spring Boot** — IoC/DI、SpringMVC、RESTful API、异常处理 | 🔴 P0 | 🔲 |
| 3 | **MySQL** — 建表/索引/查询优化/Explain | 🔴 P0 | 🔲 |
| 3 | **MyBatis** — SQL映射、动态SQL、SpringBoot整合 | 🔴 P0 | 🔲 |
| 4 | **综合项目** — SpringBoot + MySQL + MyBatis 全栈后端 | 🔴 P0 | 🔲 |
| 5 | **Redis** — 缓存策略、SpringBoot整合 | 🟡 P2 | 🔲 |
| 5 | **Linux** — 常用命令、项目部署 | 🟡 P2 | 🔲 |
| 5 | **Docker** — 容器化基础 | 🟡 P2 | 🔲 |
| 6 | **面试冲刺** — 简历 + 八股 + 项目包装 | 🟢 P3 | 🔲 |

---

**3.7 泛型** — 2026-05-27 完成

| 子章节 | 状态 | 说明 |
|--------|------|------|
| 泛型类 MagicBox\<T\> | ✅ 已完成 | 类型安全容器，编译器拦截类型错误 |
| 泛型方法 swapContent / matchTwoOffers | ✅ 已完成 | 类型参数联动，双向匹配逻辑 |
| 通配符 PECS（? extends / ? super） | ✅ 已完成 | printAllBoxes(只读→extends) + fillBoxesFrom(只写→super) |
| 泛型边界 \<T extends Comparable\<T\>\> | ✅ 已完成 | SmartBox + findMax，Object 被编译器拒之门外 |
| 类型擦除原理 | ✅ 已理解 | 擦除后 T→上界类型，非第一边界方法编译器自动强转 |
| 多重边界 & 类优先规则 | ✅ 已理解 | 类排第一保证擦除后类型可用性 |

---

**3.8 IO流** — 2026-05-28 ~ 2026-05-29 完成

| 子章节 | 状态 | 说明 |
|--------|------|------|
| File 类（路径抽象） | ✅ 已完成 | exists/getParentFile/listFiles，不操作内容 |
| 字符流（Reader/Writer） | ✅ 已完成 | BufferedReader + BufferedWriter 按行读写 |
| 字节流（InputStream/OutputStream） | ✅ 已完成 | FileBackup 三道复制 + 性能基准测试 |
| 缓冲字节流（BufferedStream） | ✅ 已完成 | 10MB 逐字节 100s vs 缓冲 23ms，4300 倍差距 |
| 字节↔字符桥梁流 | ✅ 已理解 | InputStreamReader + UTF-8 杜绝乱码 |
| PrintWriter | ✅ 已掌握 | println/printf 比 BufferedWriter 更省事 |
| 三种 write 陷阱 | ✅ 已踩坑 | write(int)写数量 / write(byte[])有垃圾 / write(byte[],0,len)唯一正解 |
| 装饰器模式 | ✅ 已理解 | 缓冲逻辑封装在类内部，接口不变能力增强 |
| 文件完整性校验 | ✅ 已完成 | 字节级逐字节比对 |
| IO 流选型决策树 | ✅ 已掌握 | 按需选类：文本/二进制/编码控制 |

---

## 学习日志

**【2026-09-07 · 打卡下班】** 线程通信（wait/notify + Condition）通关！今日你化身魔法厨房调度官，从「2 厨师 + 2 食客 + 1 口蒸笼」的乱局出发，亲手用 wait/notify 让厨房恢复秩序。

**上半场——理论三块硬骨头：** ① `wait()` 释放锁 ≠ 唤醒线程——"一睡一喊"永远成对，且必须作用在**同一把锁**上（同一个对讲机频道），睡着的线程自己醒不过来 ② 虚假唤醒——`wait` 必须包在 `while` 里，醒来重新核实条件 ③ **永久等待 ≠ 死锁**——你亲手用上一关"死锁四条件"裁决出来（锁空着但没人喊，不是死锁）。

**下半场——BunShop 实战踩坑：** 你一次写对 `while(buns.size()==capacity) + wait`，但 TODO ③ 踩进了我埋的坑——用 `notify()` 跑 15 次没卡，你没轻易放过，反问我"是不是样本量太小"。我点破真相：**容量 3 > 消费者数 2，结构性安全，根本不会死锁**。你把 `capacity` 改成 1 立刻复现卡死，并自己定位根因：`notify()` 一次只随机叫醒一个、叫错了人——被叫醒的厨师一看"还是满的"又睡回去，真正能拿包子的消费者被漏掉 → **lost wakeup → 全员睡着**。改成 `notifyAll()` 后 20 次不卡。复盘还答对了"永久等待"和"`newCondition` 分组建群消灭惊群效应"。

> 斩获徽章：【厨房调度官·初阶】- 已掌握：wait/notify 线程通信、生产者-消费者模式、while 防虚假唤醒、notifyAll 防丢失唤醒、Condition 精准唤醒。

> ☕ **本章三个核心复习要点：** ① `wait` 释放锁、`sleep` 不释放锁——wait 是"放下钥匙睡"，sleep 是"攥着钥匙睡" ② `wait` 永远包在 `while` 里——醒来重新核实条件，防虚假唤醒 + 多线程抢资源 ③ `notifyAll` 全叫醒（安全但惊群）vs `Condition` 分队列精准叫醒（高效）——`while + notifyAll` 是搭档。

> **明日挑战预告：** 你的厨房靠 wait/notify 手工协调还凑合，但如果是一场"三、二、一，点火"的火箭发射——多个线程要**同时起步**、或要等**所有子任务完成**才汇总——wait/notify 就太笨重了。下一关：**JUC 工具类**——CountDownLatch（倒计时门闩）、CyclicBarrier（循环栅栏）、Semaphore（信号量）。

---

**【2026-06-24 · 打卡下班】** 今日双线推进！线程调度收尾 + 线程池全链路贯通。

**上半场——yield与线程优先级：** 你用 `SchedulerLab` 亲手验证了 yield 和优先级的"建议性"本质——100次循环太快没触发切换，加 sleep 后两个线程完美交替五五开，最后在 CPU 密集 + 优先级拉到极端的场景下才看到高优先级赢面更大。中途你发现 `while(true)` 死循环导致程序卡住——从此牢牢记住了循环必须设退出条件。

**下半场——线程池全链路：** 你从 Executor → ExecutorService → ThreadPoolExecutor 三层接口的职责区分出发，追问了"为什么用 ExecutorService 当变量类型"——最终归结为和 `List list = new ArrayList()` 同一个多态道理。然后你亲手写出 `SettlementCenter`：`newFixedThreadPool(3)` → `submit(Callable)` → `Future.get()` → `shutdown()` + `awaitTermination()`，10笔订单并发结算一次跑通。途中踩了 `awaitTermination()` 缺参数的坑，追问了 `shutdown()` vs `shutdownNow()`、OOM 与无界队列的隐患、顺序 Future 的阻塞瓶颈——对线程池的行为边界有了完整认知。

> 斩获双徽章：【调度观察员·初阶】— 已掌握：yield 主动让出 CPU vs sleep 真正离开 RUNNABLE 的本质区别，线程优先级的"建议性"本质与 OS 差异。【并发结算官·初阶】— 已掌握：Executors 四种工厂方法、ExecutorService.submit(Callable)、Future.get() 阻塞取结果、shutdown + awaitTermination 优雅关闭。

> ☕ **今日三个核心复习要点：** ① yield 只让出 CPU 不离开 RUNNABLE，sleep 进入 TIMED_WAITING 真正让出——这是 yield 极少被用的根本原因 ② `ExecutorService pool = Executors.newFixedThreadPool(5)` 中，变量类型决定了你能调哪些方法——编译器只看声明的接口类型 ③ Future.get() 就像快递单号——当场拿到占位符，结果在后台算好后才真正"到手"，调用时阻塞等待

> ☕ **多线程（一）复习提醒（第1天）：** ① `start()` vs `run()`：前者创建新线程 JVM 调 run()，后者只是当前线程执行方法 ② Runnable 的双重缺陷：`void run()` 无返回值 + 异常传不回——这正是 Callable 要解决的 ③ sleep 不释放锁，join 等的是别的线程结束，两个都不释放锁

> **明日挑战预告：** 你的 `SchedulerLab` 里两个线程同时抢 `vipDone++` 和 `internDone++`——但万一它们在同一个纳秒同时执行 `++`，总数会不会出错？下一关：**synchronized / Lock**——线程安全的第一道门，揭开 `++` 非原子的真相。

---

**【2026-06-01 · 打卡下班】** Stream API 章通关！今日你化身魔法交易所流水线工程师，把 Lambda 章手写的 7 个 for 循环全部改写成 Stream 链式调用。从 `.filter(condition)` （直接传 Predicate）到 `.map(mapper)`（直接传 Function），你亲手验证了一个关键认知：**Stream 的中间操作不需要"包装层"——函数式接口实例直接往里传，Stream 自己会把每个元素喂给它。** 途中你撞上了 Java 类型推断的真实边界：`comparingInt(λ).reversed()` 的泛型穿透失败，你用 `(String)` 强转先让它工作，再用 `TradeAnalyzer::getPrice` 方法引用做减法——从"能编译"进化到"写得漂亮"。`mapToInt(...).sum()` 你一次写对避开了 Stream<Integer> 的装箱陷阱，`anyMatch` 的短路语义你也精准把握。最后你还精准区分了"`sorted` 必须全量缓存是天生的"和"`anyMatch` 短路是优化"——不随我的误导起舞。

斩获徽章：【Stream 流水线大师·初阶】- 已掌握：用 filter/map/sorted/distinct/limit 七种中间操作搭建数据处理流水线，区分中间操作 vs 终端操作的惰性求值机制，用方法引用 :: 写出最简 Lambda。

> ☕ **Stream 章三个核心复习要点：** ① 中间操作 vs 终端操作：返回 Stream 的是中间操作（惰性，不触发执行），返回其他类型的是终端操作（触发执行，只能一次）② `sorted()` 是有状态操作——必须缓存全部元素再排序，O(n log n)；`filter/map` 是无状态操作，来一个处理一个 ③ 方法引用 `::` 是 Lambda 的语法糖——当 Lambda 体只调用一个已存在的方法时，`TradeAnalyzer::getPrice` 等价于 `t -> getPrice(t)`，且能穿透泛型推断边界
>
> ☕ **Lambda 章复习提醒（第1天）：** ① Lambda = 函数式接口的匿名实现，目标类型由参数/赋值的目标类型推断——传给 `Predicate` 就是 `test()`，传给 `Function` 就是 `apply()` ② 四大接口分辨口诀：Predicate（判→boolean）、Function（转→R）、Consumer（消→void）、Supplier（供←无参有返）③ 局部变量捕获必须是 effectively final
>
> **明日挑战预告：** Stream 让你的 for 循环消失了——但如果一条流水线里的某个步骤可能返回 null（比如 `findFirst()`），你再也逃不掉它。下一关：**Optional**——用容器思维消灭 null 判断，告别 `if (x != null)` 的防御式地狱。

---

**【2026-06-04 · 打卡下班】** 超级整合项目「魔法交易所 3.0」正式动工！今日从零搭建了项目架构——四层分层（model/loader/analyzer/reporter）、构建依赖图（谁不依赖别人谁先写）、八份脚手架 TODO 文件。理论文档同步更新：`exchange/README.md` 新增构建顺序图，`mathrandom/README.md` 补 BigDecimal 章节（常量/构造/运算/面试题 + 金额对比陷阱），`object/README.md` 补 Objects 工具类（requireNonNull/equals/hash 防御式编程），`basics/README.md` 补 StringBuilder（循环拼接性能对比）。编码实战：`TradeStatus` 枚举 ✅（常量+字段+构造器+getter）、`Trade` 类 ✅（BigDecimal 金额+LocalDate 日期+toString）、`TradeParseException` ✅（Checked 异常双构造器）、`TradeQuery.findByProductName` ✅（Stream 过滤 + Optional.empty() 空结果）、`TradeAnalyzer.computeStats` 初版（BigDecimal sum 初始化为 null 的 NPE 已定位，待改 BigDecimal.ZERO）。途中澄清 Optional 的使用场景——Stream 管道里不能调 Optional 方法，只有终端操作 findFirst/max/min 才返回 Optional；`reduce(BigDecimal.ZERO, BigDecimal::add)` 归约操作首次接触。

> 斩获徽章：【架构设计师·初阶】— 已掌握：分层设计原则（依赖方向从底向上）、接口与抽象类的选型判断（策略模式 vs 模板方法）、DTO 与领域模型的包归属区分。

> ☕ **今日关键收获：** ① 分层构建顺序 = 依赖图倒排——model 不依赖任何人先写，ExchangeApp 依赖所有人最后写 ② `BigDecimal sum = null; sum.add(x)` → NPE！必须 `BigDecimal.ZERO` 初始化，BigDecimal 不可变运算必须接收返回值 ③ Stream 管道里不能调 Optional 方法：`stream().filter().ifPresent()` ❌，要先 `findFirst()` 拿到 Optional 再 `.ifPresent()` ④ `reduce(初始值, BinaryOperator)` 把流压缩成单个值，`BigDecimal::add` 的方法引用签名恰好匹配 `(T, T) -> T`

> **明日任务：** ① 修 `sum = null` NPE + `analyzerData` 改名挪包 ② `groupByStatus` 用 `Collectors.groupingBy` 实现 ③ `topN` 排序 + `filterByDate` ④ `FileTradeLoader` 文件加载（BufferedReader + 正则解析 + 异常包装）

---

**【2026-06-02 · 打卡下班】** Optional 章通关！今日你化身魔法交易所查询安全官，从零搭建了 `TradeQueryService`——四个查询方法全返回 `Optional<String>`，六个场景全部用链式调用消灭了 `if (x != null)`。上半场你搞清了最关键的边界：**Optional 管的是"出参缺失"，不管"入参 null"**——前者是业务常态，后者是代码 Bug，这是你从"会用 API"到"理解设计意图"的质变。下半场你逐个攻破 `map`/`filter`/`ifPresentOrElse`/`orElse` 的组合用法，亲手踩了空 Optional 上 `filter` 后分支逻辑颠倒的坑——"找安全交易"和"找危险交易"的逻辑正好相反——自己定位并修复。最后你还主动提出用哈希表优化高频查询——集合章的知识无缝复用。

斩获徽章：【查询安全官·初阶】- 已掌握：用 Optional 替代 null 返回值，链式处理"存在/缺失"双分支，区分 Optional 的职责边界，理解 `orElse`/`orElseGet`/`or`/`ifPresentOrElse` 的适用场景。

> ☕ **Optional 章三个核心复习要点：** ① Optional 管"出参缺失"不关"入参 null"——参数校验用 `Objects.requireNonNull`，业务空结果用 Optional ② `orElse` 急性质 vs `orElseGet` 惰性求值：`orElse` 的参数一定会执行，兜底计算昂贵时用 `orElseGet(() -> ...)` ③ `ifPresentOrElse(Consumer, Runnable)` 是双分支神器——Consumer 收值、Runnable 无参，和 `orElse` 族不同，它返回 void，只管副作用
>
> ☕ **Stream 章复习提醒（第1天）：** ① 中间操作 vs 终端操作：返回 Stream 的是中间操作（惰性），返回其他类型的是终端操作（触发）② `max()`/`min()`/`findFirst()` 返回 Optional——你昨天写 Stream 时没用它们，今天 Optional 章补上了这块拼图 ③ 方法引用 `::` 是 Lambda 的语法糖，但能穿透泛型推断边界
>
> **明日挑战预告：** 你从 Lambda → Stream → Optional 三章攒齐了"现代 Java 数据处理三件套"。明天不再是学新语法——你选的**超级整合项目**要把集合、IO、Lambda、Stream、Optional 五技合一，造一个真实的命令行应用。今晚想想：魔法交易所 2.0、卡牌对战系统、炼金工坊管理器——你倾向哪个方向？

---

**【2026-05-31 · 打卡下班】** Lambda 章通关！今日你从"Lambda 到底实现了谁的什么方法"的彻底困惑出发，一步步亲手写完 `TradeAnalyzer` 四大引擎——`filter`（Predicate 判读）、`extract`（Function 转换）、`forEach`（Consumer 消费）、`generate`（Supplier 生成）。你在一开始对 Lambda 和函数式接口的关系完全懵圈，但通过把 `Comparator` 的案例反复对照、把 Lambda = `test()` 实现体这个映射刻进脑海，最终自己写出了五个 Lambda 表达式——从单行 `t -> getPrice(t) > 1000` 到多行 Supplier 随机生成交易记录，无一出错。复盘阶段你亲手补上了 null 检查、ArrayList 预扩容、`count <= 0` 校验三道防线，还用 TreeSet 一步完成去重排序——集合章的知识无缝复用。途中你踩了 `while(count>0)` 死循环（count 没 --），踩了 try-catch NPE 被 `if (trades == null) return` 替代的设计选择，还自己补充了 `getBuyerName()` 工具方法。本章核心领悟：**Lambda 不是魔法，它就是在调用处当场实现函数式接口的唯一抽象方法——和匿名内部类做的事一模一样，只是语法短到只剩箭头。**

斩获徽章：【Lambda 破壁人·初阶】- 已掌握：用 Predicate/Function/Consumer/Supplier 四大接口消除样板代码，行为参数化思想入门。

> ☕ **Lambda 章三个核心复习要点：** ① Lambda = 函数式接口的匿名实现，目标类型由参数/赋值的目标类型推断——传给 `Predicate` 就是 `test()`，传给 `Function` 就是 `apply()` ② 四大接口分辨口诀：Predicate（判→boolean）、Function（转→R）、Consumer（消→void）、Supplier（供←无参有返）③ 局部变量捕获必须是 effectively final——JVM 保证 Lambda 在任何线程执行时看到的值一致
>
> ☕ **昨日 IO 章复习提醒（第3天）：** ① `read(byte[])` 返回实际读取数，`write(byte[], 0, bytesRead)` 是唯一正确的批量写入姿势 ② 装饰器模式：外层加缓冲，接口不变 ③ 跨平台文本读写用 `InputStreamReader` + `StandardCharsets.UTF_8`
>
> **明日挑战预告：** Lambda 让你的方法能接收"行为"了——但每条数据还是要手写 for 循环处理。下一章：**Stream API**——用 `.filter().map().sorted().collect()` 把 Lambda 串成流水线，8 行循环变 1 行链式调用，交易数据一键分析。

---

**【2026-05-29 · 打卡下班】** 今日双线作战！

**上半场——IO 流收官：** 你化身文件备份官，四箭齐发斩落字节流全章。`copyWithByteStream` 逐字节搬运 1 分 40 秒搬完 10MB，`copyWithBuffer` 8KB 缓冲区 25ms 搞定——你亲手证明了 4300 倍的性能鸿沟。途中你踩穿了 `write()` 的三大陷阱：`write(bytesRead)` 把数量当字节写、`write(buff)` 最后一次带垃圾、`write(buff, 0, bytesRead)` 才是正解。`BufferedStream` 初版 113ms 被你看穿是 `read(byte[])` 用错了方式，修正后 23ms 与手动缓冲平起平坐。你还亲手整顿了理论文档——补上 `BufferedInputStream/BufferedOutputStream`、桥梁流、`PrintWriter`、三种 write 陷阱辨析、选型决策树，并定下铁律：**写完文档必须自查关联类清单，舍去过时 API**。斩获徽章：【IO 破壁人】。

**下半场——Lambda 入门：** 从匿名内部类 5 行→Lambda 1 行的语法革命起步，构建了 `TradeAnalyzer` 脚手架——`Predicate` 过滤、`Function` 提取、`Consumer` 消费、`Supplier` 生成——四大函数式接口的挑战已就位，明天开写。

> ☕ **IO 章三个核心复习要点：** ① `read(byte[])` 返回实际读取数，`write(byte[], 0, bytesRead)` 是唯一正确的批量写入姿势 ② 装饰器模式：`new BufferedInputStream(new FileInputStream(path))` ——外层加缓冲，接口不变 ③ 跨平台文本读写永远用 `InputStreamReader` + `StandardCharsets.UTF_8`，别依赖 `FileReader` 的系统默认编码
>
> ☕ **Lambda 三个核心复习要点：** ① Lambda = 函数式接口的匿名实现，类型靠目标类型推断 ② 四大接口：Predicate（判）、Function（转）、Consumer（消）、Supplier（供）——从输入输出就能区分 ③ 局部变量捕获必须是 effectively final——Lambda 捕获值副本，改原变量无意义
>
> **明日挑战预告：** 写完 `TradeAnalyzer` 的 4 个 Lambda 方法后，你的数据分析引擎就活了——然后直接杀入 **Stream API**，用 `.filter().map().sorted()` 把 Lambda 串成流水线，交易数据一键分析。**但最重要的是——明天下班前，你要决定你的"超级整合项目"是什么方向。**

> ☕ **IO 流今日三个核心复习要点：** ① 三层层级：File（路径便签纸）→ FileReader/Writer（连接磁盘）→ BufferedReader/Writer（缓冲 + 便利方法） ② `readLine()` 返回 null 才算读完，赋值和判断必须在一行完成——`while ((line = br.readLine()) != null)` 是固定套路 ③ 追加模式靠 `FileWriter(path, true)` 第二个参数，不写就是清空重来
>
> **明日挑战预告：** 你的 `trades.log` 能存能读了，但每天的交易都混在一个文件里——如果想把 5 月 28 号的交易单独导出到一个 `trades_2026-05-28.log`，或者想用字节流复制文件、想看缓冲到底快多少……IO 流的下半场等着你。

---

**【2026-05-27 · 打卡下班】** 今日你化身泛型破壁人，四连击穿泛型全章。题1你用 `MagicBox<String>` / `MagicBox<Integer>` 亲手感受了编译期类型检查——如果没用泛型存 Object，类型错误要到运行时才炸。题2你实现了 `swapContent`（三行交换模板）和 `matchTwoOffers`（双向类型匹配），中间一度被 `TradeOffer` 构造器的 `Class<T>` 参数绕晕，但最终把 `String.class` 和 `"冰霜法杖"` 的区别彻底分清。题3你拿下 PECS 法则——`printAllBoxes` 读数据用 `? extends`，`fillBoxesFrom` 写数据用 `? super`，亲手体会了编译器对通配符的读写限制。题4你从零写出 `SmartBox<T extends Comparable<T>>`，用 `compareTo` 循环找最大值，写完还主动加了空参数校验和 null 元素防御。途中你对泛型边界语法产生了一连串深层追问——为什么接口也用 extends？为什么类必须排第一？擦除后接口方法会丢吗？——每一个追问都挖到了编译器的设计决策层。斩获徽章：【泛型破壁人·初阶】。

> ☕ **泛型章三个核心复习要点：** ① PECS 法则：Producer（读）用 `? extends`，Consumer（写）用 `? super`——编译器通过"捕获类型占位符"裁决每个操作是否安全 ② 类型擦除后 T 被替换为第一边界类型，`<T extends Number & Comparable>` 中 Number 必须排首位——保证擦除后默认类型自带可用方法；非第一边界的方法不会丢，编译器自动插强转 ③ `<T>` vs `?` 选择口诀：单参数只读/只写用 `?`（灵活），多参数需要类型联动用 `<T>`（绑定）
>
> **明日挑战预告：** 你的魔法交易所在内存里跑得欢，但一关电脑所有交易记录全丢。你需要把交易数据存到硬盘上——下一章：**IO流**——从文件读写到缓冲优化，让你的程序拥有"永久记忆"。

---

**【2026-05-21 · 打卡下班】** 今日你化身炼金术士，从零搭建了魔法药剂合成模拟器。上半场你用 `Random.nextInt(100)` 实现了概率判定 + 品质分级的核心引擎，中间掉进"循环里 `new AlchemyLab()`"的性能陷阱，但你亲手排查并纠正——从 `Math.random()` 底层源码到实例字段复用，你已经内化了"伪随机数"的本质。下半场你用 `Math.pow/sqrt/ceil` 三行搞定药剂定价公式，最后用 `new Random(42)` 证明了"随机"是可以复现的——三次固定种子输出完全一致，这正是调试和测试的救命稻草。斩获双徽章：【炼金术士·初阶】【炼金术士·进阶】。

> ☕ **Math & Random 章三个核心复习要点：** ① `Math.random()` 底层每次 `new Random().nextDouble()`——循环里用它是性能反模式，应声明一个 Random 实例字段复用 ② 伪随机数 = 种子 + 算法生成的确定性序列，`new Random(42)` 每次给出相同序列——这是调试的武器，不是 Bug ③ `Random.nextInt(bound)` 范围 `[0, bound)`，判定 `roll < successRate` 完成概率模拟
>
> **明日挑战预告：** 你的药剂配方名称里混进了奇怪的东西——"治疗药水!!!"、"魔 力 药 水"——你需要的不是 if-else 堆砌，而是一种叫"正则表达式"的模式匹配利器。下一章：**正则表达式**——从格式校验到文本清洗，一行代码替代十行判断。

--- 今日连下三城！上半场你化身文档考古学家，为多态、抽象类、内部类补齐了三份理论文档，每份都严格遵循「先讲"为什么需要"再给速查表」的写作规范。下半场你以魔法工坊日程管家的身份，亲手用 `java.time` API 打造了日程管理系统——`LocalTime.plusMinutes()` 计算结束时间、「不冲突条件取反」优雅检测时间段重叠、拆两行 `Comparator` 链攻破 Lambda 类型推断陷阱、`ChronoUnit.DAYS.between` 实现倒计时，最后还补上了 `Objects.requireNonNull` 的 fail-fast 防线。旧 `Date.getYear()-1900` 的设计噩梦，已被你彻底扫进历史垃圾桶。斩获徽章：【时光法师·初阶】。

> ☕ **日期时间章三个核心复习要点：** ① java.time 一切不可变，`d.plusDays(7)` 必须接收返回值，忽略返回值的静默丢弃是新手头号 Bug ② 冲突检测核心公式：`noConflict = endA <= startB || startA >= endB` —— 用不冲突条件取反比正向判断更清晰 ③ `Comparator.comparing()` 链式调用时编译器会丢失泛型类型，拆两行或显式标注类型 `(Workshop w) -> ...` 即可破解
>
> **明日挑战预告：** 魔法工坊的实验成功率忽高忽低——你需要一个随机数生成器来模拟事件概率。下一章：**Math & Random**——从掷骰子到抽卡概率，揭示伪随机数的真相。

---

**【2026-05-26 · 打卡下班】** 今日你化身炼金配方清洗师，用正则表达式三剑齐出斩落全章。挑战1你用 `trim()` + `replaceAll` 链式调用清洗脏配方名称，亲手踩了字符集里 `-` 必须放末尾的陷阱后，还主动推翻原始需求——从"只去末尾标点"升级为全位置横扫。挑战2你用 `Pattern.compile("\\d+x[\\p{L}_]+")` + `matcher.matches()` 打造原料格式校验器，7 个测试用例全对。挑战3你用 `Pattern` + `Matcher` + `find()` 循环提取数字，遇到 `1.5` 被 `\d+` 拆成 `[1, 5]` 后，用 `(\\.\\d+)?` 可选组一招制敌，一并通过整数与小数。中间一度困在"两次 new Matcher 导致 IllegalStateException"和 "if 改 while"的 debug 中，最终亲手定位修复。斩获双徽章：【药剂配方清洗师·初阶】【药剂配方清洗师·进阶】。

> ☕ **正则表达式章三个核心复习要点：** ① Pattern 是寻宝图（编译一次复用），Matcher 是探险家（轻量创建）——`str.replaceAll()` 内部每次 `Pattern.compile()`，循环里是性能反模式 ② `matches()` 是整体匹配（隐含 `^...$`），`find()` 是子串扫描可循环调用，`group()` 必须在 find() 成功后调 ③ Java 字符串里 `\` 要写 `\\`，正则里的字面小数点要写 `\\.`，`-` 在字符集里放最前或最后才安全
>
> **明日挑战预告：** 你的药剂配方数据里 `3x龙鳞` 和 `10x月光草`——它们都是"数量 x 原料名"的格式。但如果有一天，配方里出现了 `3x 龙鳞`（原料名带空格）或者 `5~10x龙鳞`（数量是个范围），你的正则该往哪个方向进化？下一章：**泛型**——给你的集合容器贴上类型标签，告别强制转型和 ClassCastException。

---

**【2026-05-19 · 打卡下班】** 今日双杀！包装类 + 异常处理两章通关。上半场你用 GoldCounter 拆解了 JVM 的装箱魔术——正则过滤格式、try-catch 兜底溢出，亲手探测了 Integer 缓存池的 -128~127 边界。下半场你化身魔法银行安全官，从零定义 `InsufficientBalanceException`，用 throw/throws/try-catch 三层防线优雅处理业务异常，并亲自验证了 Checked vs Unchecked 的本质差异——一行 `-100` 转账直接炸穿 safeTransfer，证明 IllegalArgumentException 作为程序 Bug 不该被静默吞掉。斩获双徽章：【装箱拆箱魔术师·初阶】【异常猎手·初阶】。

> ☕ **包装类三个核心复习要点：** ① 自动装箱 `Integer.valueOf()`、自动拆箱 `intValue()` —— JVM 默默做的事 ② Integer 缓存池 -128~127 内 `==` 为 true，超出为 false —— 值比较永远用 `equals()` ③ `parseInt("42")` 返回 int，`valueOf("42")` 返回 Integer（带缓存）
>
> ☕ **异常处理三个核心复习要点：** ① Checked (extends Exception) 编译器强制处理，Unchecked (extends RuntimeException) 不强制 —— 判断标准：调用方"应该"被强制处理吗？ ② `throw` 是动作（扔），`throws` 是声明（警告） ③ 关注点分离：底层方法 throw，高层方法 catch 决定怎么处理 —— 不要替调用方做决定
>
> **明日挑战预告：** 你的魔法银行里到处都是 `int` 类型的余额和金额——但如果有一天，转账金额需要精确到小数点后两位（比如 199.99 金币），`int` 还能用吗？下一章：**日期时间**——或者你想先去扫清第二阶段剩下的多态、抽象类、内部类？

---

**【2026-05-18 · 打卡下班】** Object 类通关！你从"为什么打印出来是乱码"出发，亲手重写了 toString/equals/hashCode，用卡牌图鉴系统验证了 equals-hashCode 契约——见证了两张同名同费用的牌在 HashMap 中从"重复"到"合并"的全过程。斩获徽章：【卡牌图鉴师·初阶】。

> ☕ **Object 章三个核心复习要点：** ① `==` 比较内存地址，`equals()` 比较内容——物理纸片 vs 规则判定 ② HashMap 两步查找：hashCode 定位桶 → equals 桶内精确匹配 ③ equals-hashCode 契约：equals 为 true → hashCode 必须相等，反之不强制（哈希冲突允许但影响性能）
>
> **明日挑战预告：** 你写 `collection.put(fireball, 3)` 的时候，那个 `3` 是 int 基本类型，但 HashMap 只能存对象——JVM 在背后悄悄做了什么？下一章：**包装类**——揭开自动装箱拆箱的魔法。

---

**【2026-05-17 · 打卡下班】** 枚举章节通关！你从交通灯控制系统起步，掌握了带字段/构造器的枚举，再到接口+常量特定类主体实现差异化行为，最后用单元素枚举打造了防反射攻击的全局配置。斩获三枚徽章：【信号灯指挥官·初阶】【信号灯指挥官·进阶】【单例守护者·终阶】。

> ☕ **枚举章节三个核心复习要点：** ① enum 构造器强制 private，实例数量编译期固定——所以不能用 new ② 每个常量可实现不同的方法行为（常量特定类主体）③ 枚举单例是 Java 最佳单例方案——JVM 防反射、防序列化破坏
>
> **明日挑战预告：** 你的代码里到处是 `name`、`duration`、`light` 这些名字——如果有一天你 debug 时想打印一个对象看看它到底装了啥，你会希望看到什么？下一章：`Object` 类——所有 Java 类的"祖宗"。

---

**【2026-05-14 · 打卡下班】** 第一阶段彻底收工！字符串章节归位，基础入门 8/8 全部绿灯。今日共斩获4枚徽章，集合框架6/6全章通关。

> **明日速通计划：** 第二阶段剩余4章——多态(2.4)、抽象类(2.5)、内部类(2.7)、枚举(2.8)。这些都是你大体学过的，重点放在写代码验证理解，而不是重看理论。

---

**【2026-05-14 · 下半场】** 你连下两城——用 ArrayDeque 打造任务调度中心，再用遍历与 Collections 工具四题完成集合篇章的终极试炼。获得成就：【任务调度官·初阶】【集合统御者】。

> ☕ **集合框架全章复习要点：** ① List/Set/Map/Deque 四大接口的选型决策树 ② `&&` 优先级高于 `||`——防御代码括号是好朋友 ③ peek 看一眼不动，poll 取走改变状态——副作用的本质
>
> **明日挑战预告：** 集合关底 BOSS 已斩，但别急着庆功——代码里那些整天 `+ "" +` 的字符串拼接，背后藏着一个你不知道的内存黑洞。

---

**【2026-05-14 · 上半场】** 你征服了 List 的五项试炼，后升级 WordLibrary 为 DictDemo，用 HashMap 打造了键值词典，精准拦截了重复单词和空值攻击。连斩两枚徽章：【列表领主·初阶】【词典大师·初阶】。

> ☕ **上午三个核心复习要点：** ① Map.put 相同 key 会覆盖旧值——所以加前必须 containsKey ② `&&` 优先级高于 `||`——多条件 null 检查必须加括号或全用 `||` ③ `Collections.sort()` 是集合排序的统一入口

**【2026-05-13】** 今日，你以词汇大师的身份，用 `HashSet` 构建了大小写不敏感的去重词库，成功拦截了重复单词、空值和 null 的边界攻击。获得成就：【**词典编纂者·初阶**】。

> ☕ **本章三个核心复习要点：** ① HashSet 如何通过 hashCode + equals 保证唯一性 ② LinkedList vs ArrayList vs HashSet 的选型决策 ③ 防御性编程：API 各方法 null 检查的一致性
>
> **明日挑战预告：** 词库里每个单词该配一个释义了——"apple → 苹果"。这需要一种既能存键又能查值的容器，它是什么？

---

**【2026-06-16 · 打卡下班】** 时隔11天回归，今日火力全开——先做代码复盘找回手感，然后一举拿下 loader 层，最后把整个项目推上 GitHub！

**上半场——代码复盘 & 修复：** 重新审视 `TradeAnalyzer.computeStats`，亲手定位了两个致命 Bug：① `sum.add(x)` 返回值被静默丢弃——BigDecimal 不可变运算铁律再次生效 ② 完成率分母用了 `sum`（总金额）而不是 `size`（总笔数）。讨论中你追问了 BigDecimal 频繁创建对象的性能问题——我用 IO 章逐字节 vs 缓冲做类比，引出 TLAB 指针碰撞分配的原理，以及金融级优化方案（`long` 存"分"）。`AnalyzerData` 重构三连：类名小写→大写、`totalACount`→`totalAmount`、`averageACount`→`averageAmount`、Setter 全部删除只保留 Getter——和 `Trade` 的不变对象设计保持一致。`Trade.date` 字段名规范化小写。

**下半场——loader 层从零构建：** `TradeLoader` 接口一句话声明合同，`FileTradeLoader` 实现全部解析逻辑。你亲手打通了"文件→字符串→对象"的数据全链路：`BufferedReader.readLine()` 逐行读取 → `split(",")` 切分字段 → 四个字段分别转型 → `new Trade(...)` → `list.add()`。途中你自己追问了空行/注释行的跳过逻辑（`trim().isEmpty() || startsWith("#")` → `continue`）、`TradeStatus.fromLabel` 的反向映射（`values()` 遍历 + `getLabel()` 比对 + 失败抛 `IllegalArgumentException` 的 fail-fast 设计）、异常包装与重新抛出的设计选择（`catch → throw new TradeParseException(行号, 原始异常)` 加注释 vs `catch → 打印警告 → 继续` 的容错策略）。

**编码破壁实战：** 程序一跑就炸——`fromLabel` 报"不存在的标签"，明明标签写对了。你亲手印证了 IO 章笔记里的警告：`FileReader` 用系统默认编码（GBK），`trades.log` 是 UTF-8，中文字节对不上。改成 `InputStreamReader(FileInputStream, StandardCharsets.UTF_8)` 后药到病除——这就是"字节→字符桥梁流"的真实价值，不踩坑记不住。

**GitHub 初体验：** 从 `git remote add` 到 `git add → commit → push`，学会了本地代码推到远程仓库的全流程。还配置了 `.gitignore` 排除 `out/`、`.class` 等编译产物。VPN 代理端口、PowerShell 与 Bash 语法差异——这些工具链的边边角角也一并踩过了。

> 斩获双徽章：**【日志解析官·初阶】** — 已掌握：BufferedReader + InputStreamReader(UTF-8) 读取文件、String.split 解析 CSV、字符串→枚举反向映射（values + getLabel）、Checked/Unchecked 异常包装与 fail-fast 原则。**【Git新兵·初阶】** — 已掌握：git add/commit/push/remote 基本工作流、.gitignore 配置文件排除规则。

> ☕ **今日三个核心复习要点：** ① BigDecimal 不可变——`.add()` 返回新对象，不接返回值就是静默丢弃；完成率 = 完成数÷总数，不是÷总金额 ② `FileReader` 用系统默认编码，中文环境必用 `InputStreamReader + StandardCharsets.UTF_8`——这是 IO 章桥梁流知识点的实战验证 ③ `fromLabel` 找不到匹配抛 `IllegalArgumentException`（Unchecked，fail-fast），调用方在 FileTradeLoader 的 catch 里统一包装成带行号的 `TradeParseException`——关注点分离：枚举只管语义校验，loader 管行号追踪

> ☕ **Stream 章复习提醒（第7天）：** ① `reduce(identity, BinaryOperator)` 归约操作——手写 for 循环累加的 Stream 等价写法，`BigDecimal::add` 方法引用匹配 `(T,T)→T` ② `collect(Collectors.toList())` 终端操作，`groupingBy` 分组你还没亲手写——下次推主线第一个任务

> **明日任务：** ① `groupByStatus`——用 `Collectors.groupingBy` 按 TradeStatus 分组统计 ② `topN`——sorted + limit 取前N最贵交易 ③ `filterByDate`——按日期范围过滤 ④ reporter 层——TradeReporter 抽象类 + 模板方法模式

---

## 复习提醒规则
- 每学完一个知识点后，当天、第3天、第7天分别复习一次
- 复习时重新阅读理论文档（包内README.md） + 独立重写Test练习代码

---

**【2026-06-18 · 打卡下班】** 魔法交易所 3.0 全线贯通！今日你从代码脚手架出发，亲手完成了 analyzer 三方法 + reporter 模板方法 + ExchangeApp 主入口串联——整个项目从 model → loader → analyzer → reporter → ExchangeApp 五层全链跑通。

**上半场——analyzer 收尾：** 你从 `groupByStatus` 入手，亲手拿下 `Collectors.groupingBy` → 遍历 `Map.entrySet()` → `BigDecimal reduce` 聚合的完整链路——"新 Map 空着往里 put"的设计直觉让你一次写对 LinkedHashMap 聚合模式。`topN` 用 `Comparator.comparing(Trade::getAmount).reversed()` 方法引用链一行写出排序+截断。`filterByDate` 踩了 `isAfter/isBefore` 排除边界的坑，用 `!isBefore && !isAfter` 精准实现闭区间 `[start, end]`。

**下半场——reporter 模板方法：** `TradeReporter` 从空壳进化成抽象类——`public final generate()` 锁死流程骨架，`protected abstract builtContent()` 开放扩展点，`private writeToFile` 封装文件写入。途中你遭遇 `FileWriter` 编码坑的回旋镖——IO 章学过的桥梁流在此刻二次验证：`new PrintWriter(new OutputStreamWriter(new FileOutputStream(f), StandardCharsets.UTF_8))` 才是一劳永逸的跨平台写法。`SummaryReporter` 委托 `computeStats` + `groupByStatus` 拼出汇总报表，`DetailReporter` 用 `forEach` Lambda 遍历 `topN` 拼出明细报表。你亲手澄清了"报表层本质就是字符串拼接"的认知——所有计算在 analyzer 层已完成。

**全链串联 & debug 实战：** `ExchangeApp.main` 把 loader → analyzer → query → reporter 五环串成一条线。跑通后你逐一击破：NPE（`getParentFile()` 为 null → 判空）、`BigDecimal.multiply()` 静态导入误用（删 import，实例调用）、`完成率 0.5`（忘乘 100 → `.multiply(BigDecimal.valueOf(100))`）。

> 斩获三枚徽章：**【Stream 流水线宗师·进阶】** — 已掌握：groupingBy + entrySet 分组聚合、sorted + limit 排序截断、闭区间日期过滤。**【报表架构师·初阶】** — 已掌握：模板方法模式（final 骨架 + abstract 扩展点）、OutputStreamWriter 桥梁流的双向运用（读+写）。**【魔法交易所大管家·终阶】** — 已掌握：五层分层架构从设计到全链跑通的完整流程，项目架构直觉从"依赖图倒排"到"每层只管自己"。

> ☕ **今日三个核心复习要点：** ① `groupingBy` 下游收集器一次只做一件事——同时需要笔数和总金额时，先分桶拿到 `Map<K, List<T>>`，再遍历 entrySet 用 Stream 对每组独立聚合 ② 模板方法模式三要素：`final` 骨架方法（锁流程）→ `abstract` 扩展点（子类填）→ `private` 工具方法（父类复用）③ `FileWriter` 两次踩坑验证一个真理：跨平台文本 IO 永远用 `InputStreamReader/OutputStreamWriter + StandardCharsets.UTF_8`——读如此，写亦如此

> ☕ **Stream 章复习提醒（第14天）：** ① `reduce(identity, BinaryOperator)` — `BigDecimal::add` 方法引用匹配 `(T,T)→T` ② `groupingBy` 两种形态——基础分桶 vs 带下游收集器 ③ `sorted().limit()` 链式调用——`sorted` 有状态需全量缓存，`limit` 截断下游

> ☕ **Optional 章复习提醒（第12天）：** ① `ifPresentOrElse(Consumer, Runnable)` — 双分支终端操作，比 `ifPresent + isEmpty` 更优雅 ② Optional 管出参缺失，不关入参 null——`findByProductName` 返回 `Optional<List>` 就是这个用途

> **明日挑战预告：** 魔法交易所 3.0 完成了——但你的 `SummaryReporter.buildContent` 里 `sb.append(entry.getKey().getLabel()).append(entry.getValue().getCount())...` 三个值粘一起没分隔符，输出是"完成31750.49"而不是"完成：3笔 | 合计 ¥1750.49"。第一件事修它。然后我们聊聊下一步方向——你 Java 基础的拼图只剩**多线程**一块了。

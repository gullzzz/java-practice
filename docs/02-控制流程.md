# 阶段 2：控制流程

配套代码：`src/com/practice/controlflow/ControlFlowDemo.java`

---

## 2.1 if / else if / else

```java
if (条件1) {
    // 条件1为true时执行
} else if (条件2) {
    // 条件1为false，条件2为true时执行
} else {
    // 所有条件都不满足时执行
}
```

执行顺序：**从上到下，匹配到第一个 true 就停止**。

---

## 2.2 switch（传统写法 vs 新写法）

### 传统 switch（穿透机制）

```java
switch (变量) {
    case 值1:
        // 代码
        break;  // 必须有，否则会继续执行下一个case
    case 值2:
        // 代码
        break;
    default:
        // 所有case都不匹配时执行
}
```

支持的类型：`int`/`byte`/`short`/`char`、`String`（Java 7+）、`enum`。

**fall-through（穿透）**：没有 `break` 时，会继续执行下一个 case。偶尔有意图地使用（多个 case 共享逻辑）：

```java
case 6:
case 7:
    System.out.println("周末");
    break;
```

### 增强 switch（Java 14+）

```java
// 箭头语法，不需要break
String type = switch (day) {
    case 1, 2, 3, 4, 5 -> "工作日";
    case 6, 7 -> "周末";
    default -> "无效";
};

// switch 表达式 + yield 返回值
String result = switch (day) {
    case 1 -> "周一";
    case 5 -> {
        System.out.println("TGIF!");
        yield "周五";  // 代码块中用 yield 返回值
    }
    default -> "其他";
};
```

关键改进：箭头语法消除穿透；switch 可直接作为表达式赋值。

---

## 2.3 for 循环

```java
for (初始化; 条件; 每次迭代后执行) {
    // 循环体
}

// 示例
for (int i = 0; i < 10; i++) {
    System.out.println(i);
}
```

执行顺序：**初始化 → 检查条件 → 执行体 → 迭代后操作 → 检查条件 → ...**

- `初始化` 只执行一次
- `条件` 每次循环前检查
- `迭代后操作` 每次循环体执行完后执行

### 嵌套循环

外层每执行 1 次，内层执行完整的一轮：

```java
for (int i = 1; i <= 9; i++) {        // 外层9次
    for (int j = 1; j <= i; j++) {    // 内层1到i次
        System.out.print(j + "×" + i);
    }
    System.out.println();
}
```

---

## 2.4 while 与 do-while

```java
// while：先判断，可能一次都不执行
while (条件) {
    // 循环体
}

// do-while：先执行一次，再判断（至少执行一次）
do {
    // 循环体
} while (条件);
```

选择原则：
- 知道循环次数 → `for`
- 不知道循环次数 → `while`
- 至少需要执行一次 → `do-while`

---

## 2.5 break 与 continue

| 关键字 | 作用 |
|--------|------|
| `break` | 立即**跳出**当前循环 |
| `continue` | 跳过当前迭代，进入**下一次**循环 |

### 带标签的 break/continue

```java
outer:
for (int i = 1; i <= 5; i++) {
    for (int j = 1; j <= 5; j++) {
        if (i == 3) break outer;  // 跳出外层循环
    }
}
```

用于跳出多层嵌套循环。`continue` 也可以带标签。

---

## 2.6 增强 for（for-each）

```java
int[] arr = {1, 2, 3, 4, 5};
for (int n : arr) {
    System.out.println(n);
}
```

- 无需索引，遍历所有元素
- **不能修改数组元素**（只是读到临时变量中）
- 适用于数组和所有实现 `Iterable` 接口的集合

---

## 2.7 死循环

```java
for (;;) { }         // for 死循环
while (true) { }     // while 死循环
```

需要在循环体内部用 `break` 退出。

---

## 2.8 编码建议

1. **循环变量用有意义的名字**，避免 `i`、`j` 以外的单字母
2. **避免深层嵌套**（超过 3 层），可提取方法
3. **for-each 优先于普通 for**，当不需要索引时
4. **switch 保持简单**，复杂分支逻辑考虑用 if-else 或策略模式
